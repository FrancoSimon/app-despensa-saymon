## MODIFIED Requirements

### Requirement: Admin Reports

The system SHALL show business reports for sales, products, wholesale orders, low stock, and cash register auditing.

#### Scenario: Admin opens reports with CSV exports
- **WHEN** an admin opens reports
- **THEN** CSV export controls render without passing server functions to client components
