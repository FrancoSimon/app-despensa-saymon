## MODIFIED Requirements

### Requirement: Cash Register Routes

The app SHALL expose cash register routes and shortcuts for sellers and admins.

#### Scenario: Admin filters cash registers
- **WHEN** an admin opens `/admin/cajas`
- **THEN** the app allows filtering cash registers by period, status, and operator
- **AND** detail links preserve the selected filters
