## MODIFIED Requirements

### Requirement: Cash Register History

The system SHALL show cash register shift history with opening, closing, expected payment totals, counted cash, cash difference, manual cash movements, filters, and drill-down sales detail.

#### Scenario: Admin reviews filtered register history
- **WHEN** an admin applies filters to cash register history
- **THEN** the system lists only matching cash registers
