## MODIFIED Requirements

### Requirement: Cash Register Detail Route

The app SHALL expose an admin cash register detail route that preserves safe return paths.

#### Scenario: Admin returns to originating report
- **WHEN** an admin opens a cash register detail from reports
- **THEN** the detail page shows a back link to the same reports period
