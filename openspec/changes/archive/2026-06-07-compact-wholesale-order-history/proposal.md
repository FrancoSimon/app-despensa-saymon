## Why

The wholesale portal is used on small screens, so showing many previous orders makes the page too tall and pushes the active ordering workflow down.

## What Changes

- Show only two previous wholesale orders at a time.
- Add compact previous/next controls for the order history.
- Keep the newest orders visible first.

## Impact

- Affected specs: api-surface
- Affected code: wholesale order terminal
