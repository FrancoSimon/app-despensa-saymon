## Why

The home and login screens should present SAYMON as a commerce-first experience instead of exposing internal module labels. Users enter through one login and their role determines the destination.

## What Changes

- Replace the home hero copy with a welcoming commerce message.
- Remove module labels under the home logo.
- Remove the separate wholesale portal button from the home screen.
- Replace the login side message with a short motivational greeting.

## Impact

- Affected specs: api-surface
- Affected code: home page, login page
